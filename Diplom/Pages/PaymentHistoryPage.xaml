<Page x:Class="Diplom.Pages.PaymentHistoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
      mc:Ignorable="d" 
      d:DesignHeight="750" 
      d:DesignWidth="1175"
      Title="PaymentHistoryPage">


    <Grid Background="#FFF8F0F0">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="#FFF8F0F0" BorderThickness="0,0,0,1" BorderBrush="#FFE0E0E0">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="30,0,0,0">
                    <Icon:PackIconMaterial Kind="History" Width="30" Height="30" Foreground="#FF920C0C" VerticalAlignment="Center"/>
                    <TextBlock Text="История платежей" FontSize="28" FontWeight="Bold" Foreground="#FF920C0C" VerticalAlignment="Center" Margin="15,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>
        <Grid Grid.Row="1" Margin="20,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Background="White" CornerRadius="15" Margin="5" BorderThickness="1" BorderBrush="#FFE0E0E0">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="5" ShadowDepth="1" Opacity="0.2"/>
                </Border.Effect>
                <Grid Margin="15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Background="#15920C0C" CornerRadius="10" Width="45" Height="45">
                        <Icon:PackIconMaterial Kind="CurrencyRub" Width="25" Height="25" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#FF920C0C"/>
                    </Border>
                    <StackPanel Grid.Column="1" Margin="15,0,0,0">
                        <TextBlock Text="Общая сумма" FontSize="14" Foreground="#FF666666"/>
                        <TextBlock Name="TotalSumTB" Text="150,000 ₽" FontSize="20" FontWeight="Bold" Foreground="#FF920C0C"/>
                    </StackPanel>
                </Grid>
            </Border>
            <Border Grid.Column="1" Background="White" CornerRadius="15" Margin="5" BorderThickness="1" BorderBrush="#FFE0E0E0">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="5" ShadowDepth="1" Opacity="0.2"/>
                </Border.Effect>
                <Grid Margin="15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Background="#15920C0C" CornerRadius="10" Width="45" Height="45">
                        <Icon:PackIconMaterial Kind="Receipt" Width="25" Height="25" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#FF920C0C"/>
                    </Border>
                    <StackPanel Grid.Column="1" Margin="15,0,0,0">
                        <TextBlock Text="Количество транзакций" FontSize="14" Foreground="#FF666666"/>
                        <TextBlock Name="TransactionsCountTB" Text="24" FontSize="20" FontWeight="Bold" Foreground="#FF920C0C"/>
                    </StackPanel>
                </Grid>
            </Border>
            <Border Grid.Column="2" Background="White" CornerRadius="15" Margin="5" BorderThickness="1" BorderBrush="#FFE0E0E0">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="5" ShadowDepth="1" Opacity="0.2"/>
                </Border.Effect>
                <Grid Margin="15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Background="#15920C0C" CornerRadius="10" Width="45" Height="45">
                        <Icon:PackIconMaterial Kind="ChartBar" Width="25" Height="25" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#FF920C0C"/>
                    </Border>
                    <StackPanel Grid.Column="1" Margin="15,0,0,0">
                        <TextBlock Text="Средний чек" FontSize="14" Foreground="#FF666666"/>
                        <TextBlock Name="AverageCheckTB" Text="6,250 ₽" FontSize="20" FontWeight="Bold" Foreground="#FF920C0C"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
        <Border Grid.Row="2" Background="White" CornerRadius="20" Margin="20,0,20,20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.2"/>
            </Border.Effect>
            <ListView Name="LVPaymentHistory" BorderThickness="0" Background="Transparent" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ItemContainerStyle="{StaticResource ListViewItemStyle}" d:ItemsSource="{d:SampleData ItemCount=5}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Border Background="#15920C0C" CornerRadius="10" Width="45" Height="45">
                                <Icon:PackIconMaterial Kind="Package" Width="25" Height="25" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#FF920C0C"/>
                            </Border>
                            <StackPanel Grid.Column="1" Margin="15,0">
                                <TextBlock Text="{Binding Products.ProductName}" FontWeight="SemiBold" FontSize="16" Foreground="#FF333333"/>
                                <TextBlock Text="{Binding Quantity, StringFormat=Количество: {0}}" FontSize="14" Foreground="#FF666666" Margin="0,5,0,0"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2" HorizontalAlignment="Right">
                                <TextBlock Text="{Binding Total, StringFormat={}{0} ₽}" FontWeight="Bold" FontSize="16" Foreground="#FF920C0C"/>
                                <TextBlock Text="{Binding PaymentDate, StringFormat={}{0:dd.MM.yyyy}}" FontSize="14" Foreground="#FF666666" Margin="0,5,0,0"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
    </Grid>
</Page>