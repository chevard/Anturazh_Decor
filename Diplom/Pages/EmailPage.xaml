<Page x:Class="Diplom.Pages.EmailPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
      xmlns:local="clr-namespace:Diplom.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="750" d:DesignWidth="1175"
      Title="EmailPage">

    <Grid Background="#FFF8F0F0">
        <Grid.RowDefinitions>
            <RowDefinition Height="75"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#FFF8F0F0" BorderThickness="0,0,0,1" BorderBrush="#FFE0E0E0">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="30,0,0,0">
                    <Icon:PackIconMaterial Kind="Email" Width="36" Height="36" Foreground="#FF920C0C" VerticalAlignment="Center"/>
                    <TextBlock Text="Написать администратору" FontSize="32" FontWeight="Bold" Foreground="#FF920C0C" VerticalAlignment="Center" Margin="15,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource ModernBorder}" Background="White" Margin="20">
            <Grid>
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="900" Margin="20">
                    <Border Background="#FFF8F0F0" CornerRadius="10" Padding="20" Margin="0,0,0,20">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                <Icon:PackIconMaterial Kind="TextBox" Width="30" Height="30" Foreground="#FF920C0C"/>
                                <TextBlock Text="Тема письма" FontSize="22" FontWeight="SemiBold" Foreground="#FF920C0C" Margin="12,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBox Name="SubjectTB" Height="50" Style="{StaticResource EmailTB}"/>
                        </StackPanel>
                    </Border>

                    <Border Background="#FFF8F0F0" CornerRadius="10" Padding="20" Margin="0,0,0,15">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <Icon:PackIconMaterial Kind="MessageText" Width="30" Height="30" Foreground="#FF920C0C"/>
                                <TextBlock Text="Сообщение" FontSize="22" FontWeight="SemiBold" Foreground="#FF920C0C" Margin="12,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBox Name="MessageTB" Height="180" Style="{StaticResource EmailTB}" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
                        </StackPanel>
                    </Border>

                    <Button x:Name="SendButton" Height="50" Width="200" Click="SendButton_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border CornerRadius="10" Margin="0" Padding="20,12">
                                                <Border.Background>
                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                        <GradientStop Color="#FFD73636" Offset="0"/>
                                                        <GradientStop Color="#FF920C0C" Offset="1"/>
                                                    </LinearGradientBrush>
                                                </Border.Background>
                                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                    <Icon:PackIconMaterial Kind="Send" Width="27" Height="27" VerticalAlignment="Center" Foreground="White" Margin="0,0,12,0"/>
                                                    <TextBlock Text="Отправить" Foreground="White" FontSize="20" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                                </StackPanel>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Opacity" Value="0.9"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>