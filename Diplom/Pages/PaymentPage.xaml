<Page x:Class="Diplom.Pages.PaymentPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
      mc:Ignorable="d" 
      d:DesignHeight="750" d:DesignWidth="1175" 
      Title="PaymentPage">

    <Grid Background="#FFF8F0F0">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="#FFF8F0F0" BorderThickness="0,0,0,1" BorderBrush="#FFE0E0E0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="30,0,0,0">
                    <Icon:PackIconMaterial Kind="Wallet" Width="30" Height="30" Foreground="#FF920C0C" VerticalAlignment="Center"/>
                    <TextBlock Text="Оплата" FontSize="28" FontWeight="Bold" Foreground="#FF920C0C" VerticalAlignment="Center" Margin="15,0,0,0"/>
                </StackPanel>
                <Border Grid.Column="1" Background="#FFB22A2A" CornerRadius="15" Padding="20,15" Margin="0,5,30,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Всего к оплате: " Foreground="White" FontSize="18" FontWeight="SemiBold"/>
                        <TextBlock x:Name="totaltxt" Text="0" Foreground="White" FontSize="18" FontWeight="Bold"/>
                        <TextBlock Text=" ₽" Foreground="White" FontSize="18" FontWeight="SemiBold"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
        <Grid Grid.Row="1" Margin="20,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Border Style="{StaticResource ModernBorder}" Background="White">
                <DataGrid x:Name="DG2" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Extended" Style="{DynamicResource DataGridStyle1}" CellStyle="{DynamicResource DataGridCellStyle1}" ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle1}" RowStyle="{DynamicResource DataGridRowStyle1}" Margin="10">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding ProductID}" Width="60" Visibility="Hidden"/>
                        <DataGridTextColumn Header="Название продукта" Binding="{Binding ProductName}" Width="*"/>
                        <DataGridTextColumn Header="ID категории" Binding="{Binding CategoryID}" Width="*" Visibility="Hidden"/>
                        <DataGridTextColumn Header="Категория" Binding="{Binding ProductCategories.CategoryName}" Width="*"/>
                        <DataGridTextColumn Header="Цена" Binding="{Binding Price}" Width="85"/>
                        <DataGridTextColumn Header="Количество" Binding="{Binding StockQuantity}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
                <Button x:Name="AddBtn" Content="Добавить" Style="{StaticResource ModernButton}" Background="#FFD73636" Click="AddBtn_Click">
                    <Button.Effect>
                        <DropShadowEffect BlurRadius="10" ShadowDepth="1" Opacity="0.3"/>
                    </Button.Effect>
                </Button>
                <Button x:Name="DeteleBtn" Content="Удалить" Style="{StaticResource ModernButton}" Background="#FF920C0C" Click="DeteleBtn_Click">
                    <Button.Effect>
                        <DropShadowEffect BlurRadius="10" ShadowDepth="1" Opacity="0.3"/>
                    </Button.Effect>
                </Button>
                <Button Content="Оплатить" Style="{StaticResource ModernButton}" Click="Button_Click">
                    <Button.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#FFD73636" Offset="0"/>
                            <GradientStop Color="#FF920C0C" Offset="1"/>
                        </LinearGradientBrush>
                    </Button.Background>
                    <Button.Effect>
                        <DropShadowEffect BlurRadius="10" ShadowDepth="1" Opacity="0.3"/>
                    </Button.Effect>
                </Button>
            </StackPanel>
            <Border Grid.Row="2" Style="{StaticResource ModernBorder}" Background="White" Margin="0,10,0,0">
                <DataGrid x:Name="DG" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Extended" Style="{DynamicResource DataGridStyle1}" CellStyle="{DynamicResource DataGridCellStyle1}" ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle1}" RowStyle="{DynamicResource DataGridRowStyle1}" Margin="10">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding OrderDetailID}" Width="*" Visibility="Hidden"/>
                        <DataGridTextColumn Header="ID товара" Binding="{Binding ProductID}" Width="2*" Visibility="Hidden"/>
                        <DataGridTextColumn Header="ID категории" Binding="{Binding CategoryID}" Width="2*" Visibility="Hidden"/>
                        <DataGridTextColumn Header="Название продукта" Binding="{Binding Products.ProductName}" Width="3*"/>
                        <DataGridTextColumn Header="Количество" Binding="{Binding Quantity}" Width="2*"/>
                        <DataGridTextColumn Header="Цена" Binding="{Binding Products.Price}" Width="2*"/>
                        <DataGridTextColumn Header="Стоимость" Binding="{Binding Total}" Width="2*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>
        <Border Grid.Row="2" Style="{StaticResource ModernBorder}" Background="#FFB22A2A" Margin="20,0,20,20" Padding="20,15">
            <StackPanel Orientation="Horizontal">
                <Icon:PackIconMaterial Kind="Account" Width="24" Height="24" Foreground="White" VerticalAlignment="Center"/>
                <TextBlock x:Name="TbProd" Text="Продавец:" FontSize="20" FontWeight="SemiBold" Foreground="White" Margin="10,0,0,0" VerticalAlignment="Center"/>
                <TextBlock x:Name="TBProdName" FontSize="20" FontWeight="SemiBold" Foreground="White" Margin="10,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
