<Page x:Class="Diplom.Pages.PostVKPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
      mc:Ignorable="d" 
      d:DesignHeight="750" 
      d:DesignWidth="1175"
      Title="PostVKPage">

    <Grid Background="#FFF8F0F0">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="#FFF8F0F0" BorderThickness="0,0,0,1" BorderBrush="#FFE0E0E0">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="25,0,0,0">
                    <Icon:PackIconMaterial Kind="Abacus" Width="24" Height="24" Foreground="#FF920C0C" VerticalAlignment="Center"/>
                    <TextBlock Text="Публикация в ВКонтакте" FontSize="24" FontWeight="SemiBold" Foreground="#FF920C0C" VerticalAlignment="Center" Margin="12,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>
        <Border Grid.Row="1" Background="White" CornerRadius="15" Margin="15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="8" ShadowDepth="1" Opacity="0.2"/>
            </Border.Effect>
            <Grid>
                <StackPanel MaxWidth="500" Margin="0,20,0,20" HorizontalAlignment="Center">
                    <Border Width="350" Height="200" Background="#FFF8F0F0" CornerRadius="10" BorderThickness="1" BorderBrush="#FFD73636" Margin="0,0,0,10">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="3" ShadowDepth="1" Opacity="0.1"/>
                        </Border.Effect>
                        <Image x:Name="SelectedImage" Source="/Pics/selectphoto.jpg" Stretch="Uniform" Margin="10"/>
                    </Border>
                    <Button x:Name="chooseImageBtn" Style="{StaticResource ModernButton}" Content="Выбрать фото" Click="EditBtn_Click" HorizontalAlignment="Center" Margin="0,0,0,15">
                        <Button.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#FFD73636" Offset="0"/>
                                <GradientStop Color="#FF920C0C" Offset="1"/>
                            </LinearGradientBrush>
                        </Button.Background>
                    </Button>
                    <Border Background="#FFF8F0F0" BorderThickness="1" BorderBrush="#FFD73636" CornerRadius="10" Margin="15,0,15,15">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="3" ShadowDepth="1" Opacity="0.1"/>
                        </Border.Effect>
                        <TextBox Name="TB" Height="100" Width="470" TextWrapping="Wrap" FontSize="13" Background="Transparent" BorderThickness="0" Padding="12" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" VerticalContentAlignment="Top"/>
                    </Border>
                    <Button Name="PostBtn" Style="{StaticResource ModernButton}" Content="Опубликовать" Click="PostBtn_Click" HorizontalAlignment="Center">
                        <Button.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#FFD73636" Offset="0"/>
                                <GradientStop Color="#FF920C0C" Offset="1"/>
                            </LinearGradientBrush>
                        </Button.Background>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>